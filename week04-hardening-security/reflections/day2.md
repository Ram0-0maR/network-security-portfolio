🟦 Day 2 – Remove the Attack Surface

Today’s focus was on minimizing the system’s attack surface — the collection of exposed services and inputs an attacker could exploit. I used systemctl list-units --type=service to audit running services and began removing or disabling unnecessary ones like apache2, telnet, and even sshd if not actively needed. After each change, I verified results using ss -tuln and nmap. This process emphasized the principle of least privilege — not just for users, but for system components. What stood out to me was how many services were running by default that I didn’t need, and how quietly they expose the machine to risk. Every open port is a door, and if it’s not locked — or worse, forgotten — it becomes a target. This lab taught me that hardening isn’t about adding complexity; it’s about removing everything you don’t explicitly trust. In real-world production systems, regular service audits are not optional — they’re critical to survival.

